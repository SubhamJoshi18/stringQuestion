function string6(str) {
  if (str.length < 2) {
    return str;
  } else {
    let first2char = str.substring(0, 2);
    return first2char;
  }
}

let input1 = "hello";
console.log(string6(input1));

s;
